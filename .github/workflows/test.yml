name: Test Project

on:
  pull_request: 
    branches:
      - main

jobs: 
  tests:
    runs-on: ubuntu-latest
    container:
      image: node:20
    steps:
      - uses: actions/checkout@v6
        with:
          node-version: 20
      - run: npm run build
      
