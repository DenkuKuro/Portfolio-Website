name: Deploy 

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    container:
      image: node:20
    steps:
      - uses: actions/checkout@v6
        with:
          node-version: 20
      - run: npm run predeploy
      - run: npm run deploy
